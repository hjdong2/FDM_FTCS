Explicit finite difference method for 1D and 2D heat equation using forward-time central space (FTCS) scheme in C++.

I plan to switch from Julia to C++, so I am going to convert code from classes (finite volume, finite element) writing in C++ again.

I also plan to convert our group in-house Julia code Talon to C++ in the future (if I have time:)).
